
  <div class = "container" id = "asuser">
    <% provide(:title, @user.user_id) %>
    <div class="row">
      <aside class="col-md-4">
        <section class="user_info">
          <h1>
            <%= gravatar_for @user %>
            <%= @user.user_id %>
          </h1>
        </section>
      </aside>
          <p>
             <%= flash[:notice] if flash[:notice] %>
             <%= flash[:warning] if flash[:warning] %>
          </p>
    </div>
  </div>
    