<div class = "container" id = "asuser">
    <h2>You are an Administrator.</h2>
    <% provide(:title, @currentUser.user_id) %>
    <div class="row">
      <aside class="col-md-4">
        <section class="user_info">
          <h1>
            <%= gravatar_for @currentUser %>
            <%= @currentUser.user_id %>
          </h1>
        </section>
      </aside>
    </div>
  </div>